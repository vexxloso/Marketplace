# Browser-facing API URL (no trailing slash).
#
# Local dev (API on this machine, port 4000):
NEXT_PUBLIC_API_URL=http://127.0.0.1:4000
#
# VPS behind nginx (deploy/nginx-snippet.conf) — use your public IP or domain:
# NEXT_PUBLIC_API_URL=http://203.0.113.7/market-api
